apiVersion: v1
kind: Service
metadata:
  name: {{ include "node-monitor.fullname" . }}
  labels:
    {{- include "node-monitor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "node-monitor.selectorLabels" . | nindent 4 }}
